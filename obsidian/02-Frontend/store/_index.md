---
tags: [moc, frontend]
---

# Frontend `store/` — Map of Content

> Lightweight React Context for app state — no Redux/Zustand. Holds the active run and
> the current view that drives the stepper + sidebar nav.

## Notes

- [[App State Context]] — `AppStateProvider` + `useAppState()`; `View` + `ActiveRun` (`AppState.tsx`)

## See also

- [[_HOME]] · [[02-Frontend/_index|Frontend overview]]
- [[App Entry and Router]] — consumes `view` to pick a screen
